/**************not used***************
bool approx(int old, int approx)
{
  if (approx > old *0.8 && approx < old * 1.2)
    return true;
  else 
    return false;
}
***************************************/
